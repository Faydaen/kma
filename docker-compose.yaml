version: '3'

services:

    app:
        build:
            context: ./docker/app
        image: app
        container_name: app
        hostname: kma
        volumes:
            - ./:/app
        depends_on:
            - mariadb
        environment:
            DB_HOST: mariadb
            DB_PORT: 3306
            DB_DATABASE: ${MARIADB_DATABASE}
            DB_USERNAME: ${MARIADB_USERNAME}
            DB_PASSWORD: ${MARIADB_PASSWORD}
        ports:
            - "8378:8378"
#        command: sh -c "composer install"

    mariadb:
        image: mariadb:10.11.3
        container_name: mariadb
        hostname: mariadb
        environment:
            MARIADB_DATABASE: ${MARIADB_DATABASE}
            MYSQL_USER: ${MARIADB_USERNAME}
            MARIADB_ROOT_PASSWORD: ${MARIADB_PASSWORD}
            MYSQL_PASSWORD: ${MARIADB_PASSWORD}
        ports:
            - ${MARIADB_PORT}:3306
        volumes:
            - ./docker/storages/mariadb:/var/lib/mysql
            - ./docker/mariadb/init/:/docker-entrypoint-initdb.d



#    rabbitmq:
#        image: rabbitmq:3.12.4
#        container_name: rabbitmq
#        hostname: rabbitmq
#        ports:
#            - ${RABBITMQ_AMQP_PORT}:5672  # порт для AMQP
#            - ${RABBITMQ_UI_PORT}:15672  # порт для RabbitMQ Management UI
#        volumes:
#            - ./docker/storages/rabbitmq:/var/lib/rabbitmq/mnesia

#        environment:
#            RABBITMQ_ERLANG_COOKIE: "qwerty12"  # cookie для Erlang
#    RABBITMQ_DEFAULT_USER=user -e RABBITMQ_DEFAULT_PASS=password



#

#




